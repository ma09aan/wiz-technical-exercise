iam-remove.sh
non-default.sh
remove.sh
terraform/.terraform/
# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
crash.log
*.tfvars
*.tfvars.example # Often, example files are committed, but actual tfvars files are not.
                 # If your terraform.tfvars contains only non-sensitive defaults, you might commit it.
                 # However, if it contains any secrets, ignore it.
# *.tfplan files can sometimes contain sensitive information in the diff.
*.tfplan

# Backup files
*.bak
*.backup
*.tmp

# Node.js application specific (typically within the 'app/' directory, but can be global)
app/node_modules/
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
app/.env
.env

# If you have a build step for your Node.js app that creates a dist or build folder
app/dist/
app/build/
dist/
build/

# Kubernetes - Kubeconfig files (if you ever accidentally save one in the repo)
kubeconfig
*.kubeconfig

# IDE/Editor specific files
.vscode/
.idea/
*.swp
*.swo

# Operating System specific files
# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes
# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Sensitive files - Add any specific sensitive files you might create
# For example, if you download EC2 key pairs:
*.pem

# Log files (general, if your app or scripts create them locally outside of user_data)
*.log

# GitHub Actions - Self-hosted runner files (if applicable, not for GitHub-hosted)
# _diag/
# _runner
# _actions/
