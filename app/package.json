{
  "name": "wiz-exercise-app",
  "version": "1.0.0",
  "description": "Sample web application for Wiz Technical Exercise",
  "main": "server.js",
  "scripts": {
    "start": "node server.js", # npm start will run node server.js (starts your app).npm test just exits with an error — no real tests defined yet.
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^6.0.12"
  },
  "author": "",
  "license": "ISC"
}
